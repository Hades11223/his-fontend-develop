import { useSelector } from "react-redux";

export default function useThemeCondition(array = []) {
  // const themeKey = useSelector((state) => state.tvShow.themeKey);

  // return array.find((item) => item.key === themeKey)?.value || "";
  return "";
}
