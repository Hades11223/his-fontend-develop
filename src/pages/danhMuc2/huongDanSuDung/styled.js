import styled from "styled-components";
import { Main as MainStyle } from "../styled";
export const Main = styled(MainStyle)`
  .row-actived {
    background: #c1f0db !important;
  }
  .img-upload {
    margin-right: 15px;
  }
  .custom-header {
    .home-title {
      width: 700px;
    }
  }
`;
